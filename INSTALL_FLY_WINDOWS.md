# 🪟 Установка Fly CLI на Windows

## ✅ Правильная команда для Windows PowerShell

**Используйте эту команду в PowerShell:**

```powershell
iwr https://fly.io/install.ps1 -useb | iex
```

## ❌ Неправильная команда (НЕ работает в PowerShell!)

```bash
# Эта команда НЕ работает в Windows PowerShell!
curl -L https://fly.io/install.sh | sh
```

**Причина:** В PowerShell нет команды `sh` (это Linux/Mac команда).

## 📝 Полная инструкция для Windows

### Шаг 1: Откройте PowerShell

1. Нажмите `Win + X`
2. Выберите "Windows PowerShell" или "Терминал" (Windows Terminal)

### Шаг 2: Установите Fly CLI

```powershell
# Если появится предупреждение о политике выполнения, сначала выполните:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Затем установите Fly CLI:
iwr https://fly.io/install.ps1 -useb | iex
```

### Шаг 3: Проверьте установку

```powershell
fly version
```

Должно вывести версию Fly CLI, например: `fly v0.x.x`

### Шаг 4: Войдите в аккаунт

```powershell
fly auth login
```

Это откроет браузер для авторизации.

## 🔧 Если установка не работает

### Проблема: "Политика выполнения не позволяет выполнение скриптов"

**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: "iwr не распознан"

**Решение:** Используйте полную команду:
```powershell
Invoke-WebRequest -Uri https://fly.io/install.ps1 -UseBasicParsing | Invoke-Expression
```

### Проблема: PowerShell блокирует выполнение

**Решение:** Запустите PowerShell от имени администратора или выполните:
```powershell
Set-ExecutionPolicy Bypass -Scope Process
```

## 📚 Дополнительные ресурсы

- [Официальная документация Fly.io для Windows](https://fly.io/docs/getting-started/installing-flyctl/)
- [Документация по деплою](./DEPLOY_FLY_IO.md)

---

**Готово!** Теперь вы можете использовать Fly CLI в PowerShell! 🚀
