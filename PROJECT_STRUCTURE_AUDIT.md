# Аудит структуры проекта

## 🔍 Найденные проблемы

### 1. Старые папки в корне
- ❌ `backend/` - старая версия, не используется (есть `apps/backend/`)
- ❌ `frontend/` - старая версия, не используется (есть `apps/mini-app/`)

**Рекомендация:** Удалить или переместить в архив

### 2. Документация разбросана
- ✅ Много README.md файлов в разных местах
- ✅ Отдельные MD файлы в корне (AUDIT_REPORT.md, ENV_SETUP.md и т.д.)
- ✅ Документация в модулях backend

**Рекомендация:** Собрать в папку `docs/`

### 3. Ошибка линтера
- ⚠️ `apps/mini-app/src/screens/Wallet.tsx` - Cannot find module 'react-i18next'
- ✅ Но `react-i18next` есть в `package.json`

**Рекомендация:** Переустановить зависимости

### 4. Структура проекта
- ✅ Основная структура правильная
- ✅ Все приложения в `apps/`
- ✅ Все пакеты в `packages/`
- ✅ Контракты в `contracts/`
- ✅ Инфраструктура в `infra/`

## 📋 План действий

1. ✅ Создать папку `docs/`
2. ✅ Переместить документацию в `docs/`
3. ✅ Исправить ошибку линтера
4. ✅ Удалить старые папки `backend/` и `frontend/`
5. ✅ Обновить README.md с ссылками на документацию

