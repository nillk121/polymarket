# 🚀 НАЧНИТЕ ОТСЮДА - Быстрый запуск проекта

## ✅ Что уже сделано:

1. ✅ Все зависимости установлены
2. ✅ `.env` файлы созданы для всех приложений
3. ✅ Проект готов к запуску

---

## 📝 Что нужно сделать СЕЙЧАС:

### 1. Настроить Telegram Bot Token

Откройте файл `apps/backend/.env` и `apps/telegram-bot/.env` и замените:
```
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
```

**Как получить токен:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен и вставьте в оба `.env` файла

### 2. Настроить базу данных

**📖 Подробная инструкция:** [docs/INSTALL_WITHOUT_DOCKER.md](docs/INSTALL_WITHOUT_DOCKER.md)

#### Шаг 1: Установка PostgreSQL

1. **Скачайте PostgreSQL:**
   - Официальный сайт: https://www.postgresql.org/download/windows/
   - Или: https://www.enterprisedb.com/downloads/postgres-postgresql-downloads
   - Выберите версию 14 или выше

2. **Установите PostgreSQL:**
   - Порт: `5432` (по умолчанию)
   - **Задайте пароль для пользователя `postgres` (ОБЯЗАТЕЛЬНО ЗАПОМНИТЕ!)**
   - Остальные настройки - по умолчанию

3. **Создайте базу данных через pgAdmin:**
   - Откройте **pgAdmin 4** из меню Пуск
   - Подключитесь к серверу (пароль тот, что задали)
   - Правой кнопкой на "Databases" → "Create" → "Database"
   - Имя: `polymarket` → "Save"

   **Или через командную строку:**
   ```bash
   # Откройте "SQL Shell (psql)" из меню Пуск
   # Введите пароль для postgres
   CREATE DATABASE polymarket;
   \q
   ```

4. **Обновите `apps/backend/.env`:**
   ```
   DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/polymarket?schema=public
   ```
   Замените `ВАШ_ПАРОЛЬ` на ваш пароль от PostgreSQL

#### Шаг 2: Настройка Redis (ОПЦИОНАЛЬНО)

**⚠️ Redis НЕ обязателен!** Backend будет работать без него, просто кэширование будет отключено.

**Если хотите установить Redis:**
- **Memurai** (Windows-версия Redis): https://www.memurai.com/get-memurai
- Или просто оставьте настройки Redis в `.env` как есть - backend будет работать без кэширования

### 3. Инициализировать базу данных

```bash
cd apps/backend
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed
```

### 4. Запустить проект!

**Из корня проекта:**

```bash
# Запуск всех приложений одновременно
npm run dev

# Или запуск по отдельности (в разных терминалах):
npm run dev:backend      # Backend API на http://localhost:3000/api
npm run dev:mini-app     # Mini App на http://localhost:5173
npm run dev:web-dashboard # Admin Dashboard на http://localhost:3001
npm run dev:telegram-bot  # Telegram Bot
```

---

## 🎯 Проверка работы

### Backend
Откройте в браузере: http://localhost:3000/api/health

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "...",
  "database": "connected"
}
```

### Mini App
Откройте: http://localhost:5173

### Web Dashboard
Откройте: http://localhost:3001

---

## ⚠️ Если что-то не работает:

### Backend не запускается
- Проверьте, что PostgreSQL запущен
- Проверьте `DATABASE_URL` в `apps/backend/.env`
- Убедитесь, что база данных `polymarket` создана

### Mini App не подключается к API
- Проверьте, что backend запущен на порту 3000
- Проверьте `VITE_API_URL` в `apps/mini-app/.env`

### Telegram Bot не отвечает
- Проверьте `TELEGRAM_BOT_TOKEN` в `.env` файлах
- Убедитесь, что токен правильный

---

## 📚 Подробная документация

- **[INSTALL_WITHOUT_DOCKER.md](docs/INSTALL_WITHOUT_DOCKER.md)** - ⭐ Подробная установка БЕЗ Docker
- **[QUICK_START.md](docs/QUICK_START.md)** - Подробная инструкция по запуску
- **[ENV_SETUP.md](docs/ENV_SETUP.md)** - Настройка переменных окружения
- **[README.md](README.md)** - Основная документация проекта

---

## ✅ Готово!

После выполнения этих шагов проект должен работать! 🎉

Если возникнут проблемы - смотрите раздел Troubleshooting в [QUICK_START.md](docs/QUICK_START.md)

