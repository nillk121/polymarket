# Post Templates Module

Модуль для управления шаблонами постов.

## Возможности

- ✅ Создание шаблонов постов
- ✅ Использование переменных в шаблонах
- ✅ Рендеринг шаблонов с данными
- ✅ Управление шаблонами (CRUD)

## API Endpoints

### POST /post-templates
Создание шаблона (только админ)

**Request:**
```json
{
  "name": "Новый рынок",
  "description": "Шаблон для анонса нового рынка",
  "content": "🎯 Новый рынок: {{marketTitle}}\n\n{{description}}\n\n💰 Объем: {{volume}} TON",
  "variables": {
    "marketTitle": "string",
    "description": "string",
    "volume": "number"
  },
  "isActive": true
}
```

### GET /post-templates
Получение всех шаблонов (только админ)

### GET /post-templates/:id
Получение одного шаблона (только админ)

### PUT /post-templates/:id
Обновление шаблона (только админ)

### DELETE /post-templates/:id
Удаление шаблона (только админ)

## Переменные в шаблонах

Используйте формат `{{variableName}}` для переменных:

```
🎯 {{marketTitle}}
📅 Заканчивается: {{endDate}}
💰 Объем: {{volume}} TON
```

При рендеринге переменные заменяются на реальные значения.

