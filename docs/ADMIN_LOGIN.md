# 🔐 Админ логин

## Учетные данные по умолчанию

После запуска `npm run prisma:seed` создается админ пользователь:

- **Логин:** `admin`
- **Пароль:** `admin123`

⚠️ **ВАЖНО:** Обязательно измените пароль в production!

## Как войти в админ панель

1. Откройте админ панель: http://localhost:3001/login
2. Введите:
   - Имя пользователя: `admin`
   - Пароль: `admin123`
3. Нажмите "Войти"

## Изменение пароля админа

### Через базу данных

```sql
-- В pgAdmin или psql
-- Сначала нужно захешировать новый пароль через bcrypt
-- Или используйте скрипт ниже
```

### Через seed файл

Отредактируйте `apps/backend/prisma/seed.ts`:

```typescript
const adminPassword = 'ваш_новый_пароль';
```

Затем запустите:
```bash
cd apps/backend
npm run prisma:seed
```

### Через API (если будет реализовано)

В будущем можно добавить endpoint для смены пароля админа.

---

## Безопасность

1. **Никогда не коммитьте пароли в git!**
2. **Используйте сильные пароли в production**
3. **Регулярно меняйте пароли**
4. **Используйте 2FA для админ доступа (в будущем)**

---

## Troubleshooting

### Ошибка: "Неверный логин или пароль"

1. Убедитесь, что seed был запущен: `npm run prisma:seed`
2. Проверьте, что пользователь создан в базе данных
3. Проверьте правильность логина и пароля

### Ошибка: "Доступ запрещен. Требуется роль администратора"

1. Убедитесь, что пользователь имеет роль `admin`
2. Проверьте в базе данных таблицу `user_roles`

---

**Последнее обновление:** 2024

